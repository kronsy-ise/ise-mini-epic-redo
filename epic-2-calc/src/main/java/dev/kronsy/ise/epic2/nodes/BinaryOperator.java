package dev.kronsy.ise.epic2.nodes;

/**
 * A Binary Operator is an arithmetic operation that operates on two operands 
 * the *left hand side* (lhs) and the *right hand side* (rhs)
 *
 */
public enum BinaryOperator{
  Add,
  Sub,
  Mul,
  Div,
  Pow,
}
